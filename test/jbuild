(jbuild_version 1)

(executable
  ((name model_building)
   (libraries (decml))
   (preprocess (pps (decml.ppx)))
  ))

(alias 
  ((name test-model_building)
   (deps (model_building.exe))
   (action  (run ${<}))))
